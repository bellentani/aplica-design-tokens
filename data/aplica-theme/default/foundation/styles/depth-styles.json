{
  "depth": {
    "level": {
      "oneMinus": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.micro}",
          "spread": "{semantic.depth.proximity.flat}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.inner}"
        },
        "$description": "Token de Profundidade para nível negativo interno, usado para dar senso de elementos com conteúdo interno (como formulários)."
      },
      "zero": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.micro}",
          "spread": "{semantic.depth.proximity.flat}",
          "color": "{semantic.opacity.color.grayscale.superTranslucid}"
        },
        "$description": "Esse é o token de valor zero para profundidade. Normalmente ele é invocado em todos os componentes que possam usar um componente Box como agrupador. Motivo: isso facilita criar animações de relevo quando outros agrupadores de níveis diferentes são chamados."
      },
      "one": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.micro}",
          "spread": "{semantic.depth.proximity.flat}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        }
      },
      "two": {
        "description": "",
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.extraSmall}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        }
      },
      "three": {
        "description": "",
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.medium}",
          "spread": "{semantic.depth.proximity.medium}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        }
      },
      "four": {
        "description": "",
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.large}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        }
      },
      "five": {
        "description": "",
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.extraLarge}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        }
      },
      "six": {
        "description": "",
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.mega}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        }
      }
    }
  }
}