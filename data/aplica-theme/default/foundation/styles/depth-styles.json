{
  "depth": {
    "level": {
      "oneMinus": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.micro}",
          "spread": "{semantic.depth.proximity.flat}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.inner}"
        },
        "$description": "Negative internal depth token, used to give a sense of elements with inner content (such as forms)."
      },
      "zero": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.micro}",
          "spread": "{semantic.depth.proximity.flat}",
          "color": "{semantic.opacity.color.grayscale.superTranslucid}"
        },
        "$description": "This is the zero value token for depth. It is usually invoked in all components that may use a Box component as a wrapper. Reason: this facilitates creating elevation animations when other wrappers of different levels are called."
      },
      "one": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.micro}",
          "spread": "{semantic.depth.proximity.flat}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        },
        "$description": "Depth token for level one, used for subtle elevation of elements above the base surface."
      },
      "two": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.extraSmall}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        },
        "$description": "Depth token for level two, used for slightly more prominent elevation, such as cards or raised buttons."
      },
      "three": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.medium}",
          "spread": "{semantic.depth.proximity.medium}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        },
        "$description": "Depth token for level three, used for elements that need to stand out more, such as modals or floating panels."
      },
      "four": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.large}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        },
        "$description": "Depth token for level four, used for high prominence elements, such as overlays or dialogs."
      },
      "five": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.extraLarge}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        },
        "$description": "Depth token for level five, used for very prominent elements that must appear above most content."
      },
      "six": {
        "$type": "boxShadow",
        "$value": {
          "x": "{semantic.depth.distance.flat}",
          "y": "{semantic.depth.distance.flat}",
          "blur": "{semantic.depth.distance.mega}",
          "spread": "{semantic.depth.proximity.close}",
          "color": "{semantic.opacity.color.grayscale.superTransparent}",
          "type": "{semantic.depth.position.outter}"
        },
        "$description": "Depth token for level six, used for the highest elevation, typically reserved for temporary or critical UI elements."
      }
    }
  }
}